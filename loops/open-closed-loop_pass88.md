## Pass 88
- Reviewed design in `CRM/README.md` against existing extensions.
- Added missing `nucleus-suppliers` entry in `docs/inventory.md`.
- Updated CRM README bullet list to mention supplier registration.
- Ran `pnpm install` and `npm test`; build fails with `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL` in app build.
